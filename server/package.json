{
  "name": "octra-wallet-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run index.ts",
    "build": "bun build --compile --outfile=wallet-generator ./index.ts",
    "start": "bun run index.ts"
  },
  "dependencies": {
    "bip39": "^3.1.0",
    "tweetnacl": "^1.0.3"
  },
  "devDependencies": {
    "@types/bun": "^1.2.16",
    "typescript": "^5.3.3"
  }
}